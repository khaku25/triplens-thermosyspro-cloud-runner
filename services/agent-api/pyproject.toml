[project]
name = "triplens-agent-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.117.1",
  "python-multipart>=0.0.20",
  "google-genai>=2.3.0",
]

[tool.vercel]
entrypoint = "app:app"
